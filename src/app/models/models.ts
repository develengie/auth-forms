export interface SigninData {
    email: string;
    password: string;
}

export interface SignupData {
    name: string;
    nickname: string;
    email: string;
    sex: string;
    password: string;
    confirmPassword: string;
}
